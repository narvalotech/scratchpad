# Live-reloading slint UI viewer

slintpad is great but I like my editor comforts 😁

Invoke with

``` sh
cargo run -- my-app-window.slint
```

It will compile and display the `AppWindow` component.

The UI should reload when the .slint file is saved.
