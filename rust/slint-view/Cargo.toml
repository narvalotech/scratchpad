[package]
name = "slint-view"
version = "0.1.0"
edition = "2021"

[dependencies]
# Enable the preview-lre feature here
slint-interpreter = { version = "1.9.0", features = ["display-diagnostics", "internal-live-preview"] }
slint = "1.9.0"
spin_on = "0.1.1"
